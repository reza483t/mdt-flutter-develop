import 'package:flutter/material.dart';






class relays_Page extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(  title: Text(' relays page'),),
      body: Column(children: [
        Text('data')
      ]),
    );
  }
}